name: Veedor CI

on:
  pull_request:

permissions:
  pull-requests: write

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: veedor/veedor-ci@main
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          comment-on-pr: true
